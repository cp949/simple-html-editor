# HTML 편집

서버 HTML을 불러와 사람이 편집하고 저장할 HTML로 전달하는 라이브러리 컨텍스트다. 편집기와 이를 사용하는 애플리케이션 사이의 데이터 책임을 같은 언어로 구분한다.

## Language

**저장 HTML**:
소비자가 영속 저장 대상으로 취급하는 HTML 문자열이다. 빈 문서와 내용이 있는 문서를 구분한다.
_Avoid_: 저장 원본, 원본 HTML

**정규화 HTML**:
편집기가 지원하는 문서 의미로 읽고 일관된 형태로 표현한 HTML이다. 입력 HTML과 바이트 단위로 같음을 뜻하지 않는다.
_Avoid_: 원문 HTML, 무손실 HTML

**빈 문서**:
텍스트도 이미지나 표 같은 의미 있는 콘텐츠도 없는 문서다. 공백이나 빈 문단의 존재만으로 내용이 있다고 보지 않는다.
_Avoid_: 빈 문자열

**소비자**:
편집기에 값을 제공하고 변경 결과의 저장, 네트워크 흐름과 이미지 처리를 소유하는 애플리케이션이다.
_Avoid_: 에디터 서버, 저장 엔진

**Human test**:
사람이 대상 애플리케이션과 실제 브라우저에서 대표 조회, 편집, 저장과 재조회 흐름을 확인하는 검증이다. 자동화된 브라우저 테스트와 구분한다.
_Avoid_: 자동 E2E, 단위 테스트
